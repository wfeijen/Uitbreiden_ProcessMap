---
title: "Untitled"
author: "Willem Feijen"
date: "16 februari 2018"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(bupaR)
library(edeaR)
library(processmonitR)
library(plotluck)
library(DiagrammeR)
library(data.table)
#library(DiagrammeRsvg)
library(anytime)
library(processmapR)

csvLog<-read.table("../largeFile.csv",header = TRUE, sep = ";",na = c("n.v.t.","onbekend","-", "NA"))
csvLog$timeStamp <- anydate(csvLog$timeStamp)#as.POSIXct(strptime(csvLog$timeStamp, "%d-%m-%Y"))
```
We houden: `r nrow(csvLog)` regels van de `r lengteLogVoorVerwijderen` regels uit de oorspronkelijke log over, ofwel `r round(nrow(csvLog)*100/lengteLogVoorVerwijderen,digits = 2)`%.

#Gevonden kolomkoppen:
```{r echo=FALSE, warning=T, error=T, message=T}
colnames(csvLog)
```

#Omzetten naar een processmining Log 
```{r echo=FALSE, warning=FALSE}
csvLog <- csvLog[complete.cases(csvLog[ , c("CaseId","status","timeStamp")]),]

if (!("Werknemer" %in% colnames(csvLog))){csvLog$Werknemer<-NA}
eventLog.Base<-eventlog(csvLog,
        case_id = "CaseId",
        activity_id = "Activiteit",
        activity_instance_id = "activity_instance",
        lifecycle_id = "status",
        timestamp = "timeStamp",
        resource_id = "Werknemer"
    )
```
# Map met frequency
```{r}
agregationInst = list( frequency(value = "absolute", columnName = "xxx") )
map<-enrichedProcessMap(eventLog.Base, aggregationInstructions = agregationInst )

map.nodes <- map %>% get_node_df()
map.edges <- map %>% get_edge_df()

map %>%
    processmapR::color_activities( column = "xxx",
                      colorCaption = "black",
                      colorUpperbound = "deeppink",
                      colorLowerbound = "green1") %>%
    label_activities(columns =c("activity_name","xxx")) %>%
          color_transitions(column = "xxx",
          colorCaption = "black",
          colorUpperbound = "deeppink",
          colorLowerbound = "darkgreen") %>%
    label_transitions(columns = c("xxx")) %>% 
    render_graph()
```
# Map met performance
```{r}
agregationInst = list( performance(FUN = mean, columnName = "xxx"))
map<-enrichedProcessMap(eventLog.Base, aggregationInstructions = agregationInst )
map %>%
    processmapR::color_activities( column = "xxx",
                      colorCaption = "black",
                      colorUpperbound = "deeppink",
                      colorLowerbound = "green1") %>%
    label_activities(columns =c("activity_name","xxx")) %>%
          color_transitions(column = "xxx",
          colorCaption = "black",
          colorUpperbound = "deeppink",
          colorLowerbound = "darkgreen") %>%
    label_transitions(columns = c("xxx")) %>% 
    render_graph()

map.nodes <- map %>% get_node_df()
map.edges <- map %>% get_edge_df()
```
# Map met aggregate
```{r}
eventLog.Base$aantal_kamers <- as.numeric(as.character(eventLog.Base$aantal_kamers))

agregationInst = list(columnAggregate( FUN = mean, columnNameIn = "aantal_kamers", columnNameOut = "aantal_kamers", edgeOperation = "to"),columnAggregate( FUN = mean, columnNameIn = "totaal_oppervlakte", columnNameOut = "totaal_oppervlakte", edgeOperation = "from")
                      )
map<-enrichedProcessMap(eventLog.Base, aggregationInstructions = agregationInst )
map.nodes <- map %>% get_node_df()
map.edges <- map %>% get_edge_df()
map %>%
    processmapR::color_activities( column = "xxx2",
                      colorCaption = "black",
                      colorUpperbound = "deeppink",
                      colorLowerbound = "green1") %>%
    label_activities(columns =c("activity_name","xxx2")) %>%
          color_transitions(column = "xxx2",
          colorCaption = "black",
          colorUpperbound = "deeppink",
          colorLowerbound = "darkgreen") %>%
    label_transitions(columns = c("xxx2")) %>% 
    render_graph()


```
```{r}
map %>% get_node_df()

map %>%
    processmapR::color_activities( column = "aantal_kamers",
                      colorCaption = "black",
                      colorUpperbound = "deeppink",
                      colorLowerbound = "green1") %>%
    label_activities(columns =c("activity_name")) %>%
          color_transitions(column = "xxx2",
          colorCaption = "black",
          colorUpperbound = "deeppink",
          colorLowerbound = "darkgreen") %>%
    label_transitions(columns = c("xxx2")) %>% 
    render_graph()
```

```{r}
map %>%
    color_activities( column = "xxx2",
                      colorCaption = "black",
                      colorUpperbound = "lightblue",
                      colorLowerbound = "green1") %>%
    label_activities(columns =c("activity_name","xxx1","xxx2")) %>%
    color_transitions(column = "xxx2",
          colorCaption = "black",
          colorUpperbound = "deeppink",
          colorLowerbound = "darkgreen") %>%
    label_transitions(columns = c("xxx1","xxx2")) %>% 
    render_graph()
```

```{r}
map %>%
    color_activities( column = "xxx2",
                      colorCaption = "black",
                      colorUpperbound = "yellow",
                      colorLowerbound = "green1") %>%
    label_activities(columns =c()) %>%
    color_transitions(column = "xxx2",
          colorCaption = "black",
          colorUpperbound = "deeppink",
          colorLowerbound = "darkgreen") %>%
    label_transitions(columns = c()) %>% 
    render_graph()
```


```{r}

```

